# mongodb-sharded-cluster-docker
Infrastructure lab for deploying a fully sharded MongoDB cluster using Docker. Includes manual setup (docker run), Docker networks, replica sets, config servers, mongos router, and dev/prod
